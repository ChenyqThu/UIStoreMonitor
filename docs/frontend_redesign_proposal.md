# UniFi Store Monitor 前端展示重构方案

> 创建时间: 2025-11-21
> 状态: 已审阅

---

## 一、当前前端结构分析

### 1.1 页面结构

```
App.tsx
├── Layout.tsx (Navbar + Footer)
├── Dashboard.tsx (/)           # 产品列表页
└── ProductDetail.tsx (/product/:id)  # 产品详情页
```

### 1.2 当前 Dashboard 功能

| 功能 | 实现方式 | 问题 |
|------|----------|------|
| 产品列表 | 表格展示 | 只展示单一价格，无变体信息 |
| 类别筛选 | 按钮组 | 无促销/折扣筛选 |
| 搜索 | 文本框 | 仅搜索名称和 SKU |
| 排序 | 下拉选择 | 无法按折扣排序 |

**当前表格列**：
- Product (名称 + SKU + 图片)
- Category
- Subcategory
- Stock Status (In Stock / Out of Stock)
- Price (单一价格)
- Last Check
- Actions (链接)

### 1.3 当前 ProductDetail 功能

| 功能 | 实现方式 |
|------|----------|
| 基本信息 | 图片、名称、SKU、价格、库存状态 |
| 价格历史 | AreaChart (recharts) |
| 库存历史 | Step AreaChart |

### 1.4 当前数据模型

```typescript
interface Product {
  id: string;        // 实际是 SKU
  name: string;
  sku: string;
  category: string;
  subcategory?: string;
  currentPrice: number;  // 单一价格
  currency: string;
  inStock: boolean;
  imageUrl: string;
  url: string;
  lastUpdated: string;
  history: ProductHistory[];  // 产品级别历史
}
```

---

## 二、当前问题总结

### 2.1 数据展示不完整

| 问题 | 影响 |
|------|------|
| **无折扣信息** | 无法知道哪些产品在打折、折扣多少 |
| **无变体展示** | Cable 产品 24 个变体只显示 1 个 |
| **无促销标签** | Black Friday 等促销产品无法识别 |
| **无产品描述** | 只有名称，无完整标题和描述 |

### 2.2 监控功能缺失

| 问题 | 影响 |
|------|------|
| **无价格变化高亮** | 无法快速发现价格变动 |
| **无库存变化通知** | 无法看到补货/缺货变化 |
| **无折扣变化追踪** | 无法知道折扣何时开始/结束 |
| **无统计概览** | 缺少整体数据汇总 |

### 2.3 用户体验问题

| 问题 | 影响 |
|------|------|
| **筛选维度单一** | 只能按类别筛选，无法按库存/折扣筛选 |
| **排序选项不足** | 无法按折扣幅度排序 |
| **详情页信息有限** | 无法看到变体选择和各变体价格 |

---

## 三、新前端展示方案

### 3.1 整体架构

```
App.tsx
├── Layout.tsx
│   ├── Navbar.tsx (增加统计徽章)
│   └── Footer.tsx
│
├── Dashboard.tsx (/)
│   ├── StatsOverview        # 新增：统计卡片
│   ├── FilterBar            # 增强：多维筛选
│   ├── ProductTable         # 增强：折扣/变体信息
│   └── QuickActions         # 新增：快捷操作
│
├── ProductDetail.tsx (/product/:id)
│   ├── ProductInfo          # 增强：完整信息
│   ├── VariantSelector      # 新增：变体选择器
│   ├── PriceChart           # 增强：含折扣历史
│   ├── StockChart           # 保持
│   └── DiscountHistory      # 新增：折扣历史
│
├── DealsPage.tsx (/deals)   # 新增：专门的折扣页面
│   ├── ActiveDeals          # 当前折扣
│   └── DealHistory          # 历史折扣
│
└── AlertsPage.tsx (/alerts) # 新增：变化提醒页面
    ├── PriceChanges         # 价格变化
    ├── StockChanges         # 库存变化
    └── NewDiscounts         # 新折扣
```

### 3.2 新数据模型

```typescript
// 产品（对应 products 表）
interface Product {
  id: string;              // UUID
  name: string;
  title: string;           // 新增：完整标题
  shortDescription: string; // 新增：描述
  slug: string;
  categorySlug: string;
  subcategoryId: string;
  collectionSlug: string;
  imageUrl: string;
  url: string;
  status: string;          // 'Available' | 'Sold Out'
  minPrice: number;        // 最低变体价格
  maxPrice: number;        // 最高变体价格
  currency: string;
  hasDiscount: boolean;    // 新增：是否有折扣
  createdAt: string;
  lastUpdated: string;
}

// 产品变体（对应 product_variants 表）
interface ProductVariant {
  id: number;
  productId: string;
  sku: string;
  displayName: string;     // '1m White'
  currentPrice: number;
  regularPrice: number | null;  // 原价
  discountPercent: number | null;
  currency: string;
  inStock: boolean;
  status: string;
  isVisible: boolean;
  lastUpdated: string;
}

// 产品标签（对应 product_tags 表）
interface ProductTag {
  id: number;
  productId: string;
  tagName: string;
  tagType: string;    // 'feature' | 'promo' | 'poe' | etc.
  tagValue: string;
}

// 产品选项（对应 product_options 表）
interface ProductOption {
  id: number;
  productId: string;
  optionTitle: string;    // 'Color' | 'Length'
  optionValues: string[]; // ['White', 'Blue', 'Black']
}

// 变体历史（对应 variant_history 表）
interface VariantHistory {
  id: number;
  variantId: number;
  sku: string;
  price: number;
  regularPrice: number | null;
  discountPercent: number | null;
  inStock: boolean;
  status: string;
  recordedAt: string;
}

// 聚合的产品详情（前端使用）
interface ProductDetail extends Product {
  variants: ProductVariant[];
  tags: ProductTag[];
  options: ProductOption[];
}
```

---

## 四、页面详细设计

### 4.1 Dashboard 页面改进

#### 4.1.1 统计概览卡片（新增）

```
┌─────────────────────────────────────────────────────────────────────┐
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐│
│  │ 总产品   │  │ 在售     │  │ 缺货     │  │ 打折中   │  │ 新上架 ││
│  │   523    │  │   412    │  │   111    │  │   28     │  │   5    ││
│  │          │  │  78.8%   │  │  21.2%   │  │ 最高65%  │  │ 本周   ││
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  └────────┘│
└─────────────────────────────────────────────────────────────────────┘
```

**统计数据来源**：
```sql
-- 产品总数
SELECT COUNT(*) FROM products;

-- 在售/缺货
SELECT
  COUNT(*) FILTER (WHERE status = 'Available') as available,
  COUNT(*) FILTER (WHERE status != 'Available') as unavailable
FROM products;

-- 打折产品
SELECT COUNT(DISTINCT product_id)
FROM product_variants
WHERE discount_percent > 0;

-- 本周新上架
SELECT COUNT(*) FROM products
WHERE created_at > NOW() - INTERVAL '7 days';
```

#### 4.1.2 增强筛选栏

```
┌─────────────────────────────────────────────────────────────────────┐
│ [🔍 搜索产品...]                                                    │
├─────────────────────────────────────────────────────────────────────┤
│ 类别: [All ▾]  库存: [All ▾]  折扣: [All ▾]  排序: [名称 A-Z ▾]    │
│                                                                     │
│ 快捷筛选: [🔥 Black Friday] [💰 有折扣] [✅ 有货] [🆕 新品]         │
└─────────────────────────────────────────────────────────────────────┘
```

**筛选选项**：

| 筛选器 | 选项 |
|--------|------|
| 库存状态 | All / In Stock / Out of Stock |
| 折扣状态 | All / On Sale / No Discount |
| 排序 | Name (A-Z/Z-A) / Price (Low-High/High-Low) / Discount (High-Low) / Recently Updated |

**快捷筛选标签**：
- `Black Friday` - 带有 `black-friday` 标签的产品
- `有折扣` - `hasDiscount = true` 的产品
- `有货` - `status = 'Available'` 的产品
- `新品` - 最近 7 天新增的产品

#### 4.1.3 增强产品表格

```
┌────────────────────────────────────────────────────────────────────────────────┐
│ Product           │ Category      │ Price          │ Stock   │ Updated │      │
├───────────────────┼───────────────┼────────────────┼─────────┼─────────┼──────┤
│ [img] UCG-Max     │ Cloud Gateway │ $179           │ ✅ In   │ 2h ago  │ [>]  │
│       UCG-Max     │               │ ~~$279~~ -36%  │  Stock  │         │      │
│       🔥 BF Deal  │               │                │         │         │      │
├───────────────────┼───────────────┼────────────────┼─────────┼─────────┼──────┤
│ [img] Patch Cable │ Accessories   │ $2 - $92       │ ✅ In   │ 2h ago  │ [>]  │
│       24 variants │               │ 24 SKUs        │  Stock  │         │      │
├───────────────────┼───────────────┼────────────────┼─────────┼─────────┼──────┤
│ [img] UDM-Pro     │ Cloud Gateway │ $379           │ ❌ Out  │ 1d ago  │ [>]  │
│       UDM-Pro     │               │                │ of Stock│ ⚠️ 缺货 │      │
└────────────────────────────────────────────────────────────────────────────────┘
```

**表格列改进**：

| 列 | 内容 | 改进点 |
|-----|------|--------|
| Product | 图片 + 名称 + SKU + 标签 | 添加促销标签、变体数量 |
| Category | 类别 | 保持 |
| Price | 价格区间 / 折扣价 | **显示原价划线 + 折扣百分比** |
| Stock | 库存状态 | 添加状态变化提示 |
| Updated | 更新时间 | 添加变化高亮 |
| Actions | 操作按钮 | 保持 |

**价格显示逻辑**：
```typescript
// 单变体产品
if (variants.length === 1) {
  if (discountPercent) {
    return `$${currentPrice} (was $${regularPrice}) -${discountPercent}%`;
  }
  return `$${currentPrice}`;
}

// 多变体产品
return `$${minPrice} - $${maxPrice} (${variants.length} SKUs)`;
```

### 4.2 ProductDetail 页面改进

#### 4.2.1 布局结构

```
┌─────────────────────────────────────────────────────────────────────┐
│ ← Back to Dashboard                                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────┐   ┌────────────────────────────────────────┐   │
│  │                │   │ Cloud Gateway Max                      │   │
│  │    [IMAGE]     │   │ UCG-Max                                │   │
│  │                │   │                                        │   │
│  │                │   │ Compact 2.5G Cloud Gateway with 30+    │   │
│  │                │   │ UniFi device support...                │   │
│  │                │   │                                        │   │
│  └────────────────┘   │ ┌────────────────────────────────────┐ │   │
│                       │ │ 变体选择器 (如果有多变体)           │ │   │
│                       │ │ Color: [White] [Blue] [Black]      │ │   │
│                       │ │ Length: [1m] [2m] [3m] [5m] ...    │ │   │
│                       │ └────────────────────────────────────┘ │   │
│                       │                                        │   │
│                       │ 价格: $179  原价: $279  折扣: -36%     │   │
│                       │ 🔥 Black Friday Deal                   │   │
│                       │                                        │   │
│                       │ ✅ In Stock                            │   │
│                       │ [View on UI Store →]                   │   │
│                       └────────────────────────────────────────┘   │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ 变体价格表 (多变体产品显示)                                   │  │
│  │ ┌──────────────┬─────────┬─────────┬────────┬───────────┐    │  │
│  │ │ SKU          │ Option  │ Price   │ Status │ Discount  │    │  │
│  │ ├──────────────┼─────────┼─────────┼────────┼───────────┤    │  │
│  │ │ U-Cable-1M   │ 1m Wht  │ $2.60   │ ✅     │ -         │    │  │
│  │ │ U-Cable-2M   │ 2m Wht  │ $3.40   │ ✅     │ -         │    │  │
│  │ │ U-Cable-1M-BL│ 1m Blue │ $2.60   │ ❌     │ -         │    │  │
│  │ └──────────────┴─────────┴─────────┴────────┴───────────┘    │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│  Tab: [价格历史] [库存历史] [折扣历史] [产品特性]                   │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                                                              │  │
│  │     Price History Chart (支持选择具体变体)                    │  │
│  │     [SKU 选择器: All / UCG-Max / ...]                        │  │
│  │                                                              │  │
│  │     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~              │  │
│  │    /                                           \             │  │
│  │   /                                             \_____       │  │
│  │  $279 ------------------------------------------ $179        │  │
│  │                                                              │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

#### 4.2.2 变体选择器组件（新增）

```typescript
interface VariantSelectorProps {
  options: ProductOption[];
  variants: ProductVariant[];
  selectedVariant: ProductVariant | null;
  onSelect: (variant: ProductVariant) => void;
}

// 使用示例
<VariantSelector
  options={[
    { optionTitle: 'Color', optionValues: ['White', 'Blue', 'Black'] },
    { optionTitle: 'Length', optionValues: ['1m', '2m', '3m', '5m', '8m'] }
  ]}
  variants={variants}
  selectedVariant={selectedVariant}
  onSelect={handleVariantSelect}
/>
```

#### 4.2.3 变体价格表组件（新增）

多变体产品显示所有变体的价格表：

```typescript
interface VariantTableProps {
  variants: ProductVariant[];
  options: ProductOption[];
}

// 表格列
// SKU | Options (动态列) | Price | Regular Price | Discount | Stock
```

#### 4.2.4 增强价格历史图表

```typescript
// 支持按 SKU 筛选
interface PriceChartProps {
  variants: ProductVariant[];
  history: VariantHistory[];
  selectedSku: string | 'all';  // 'all' 显示所有变体
}

// 图表功能
// - 多条线（每个 SKU 一条）
// - 折扣期间用不同颜色标记
// - Tooltip 显示详细信息（价格、原价、折扣%）
```

#### 4.2.5 折扣历史标签页（新增）

```
┌─────────────────────────────────────────────────────────────────┐
│ 折扣历史                                                        │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────┬────────┬────────┬────────┬───────────────────┐ │
│ │ 日期        │ 原价   │ 折扣价 │ 折扣%  │ 状态              │ │
│ ├─────────────┼────────┼────────┼────────┼───────────────────┤ │
│ │ 2025-11-21  │ $279   │ $179   │ -36%   │ 🔥 进行中         │ │
│ │ 2025-11-15  │ $279   │ $229   │ -18%   │ 已结束            │ │
│ │ 2025-10-01  │ -      │ $279   │ -      │ 无折扣            │ │
│ └─────────────┴────────┴────────┴────────┴───────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

#### 4.2.6 产品特性标签页（新增）

```
┌─────────────────────────────────────────────────────────────────┐
│ 产品特性 (来自 tags)                                            │
├─────────────────────────────────────────────────────────────────┤
│ 网络特性:                                                       │
│   [10G SFP+] [2.5 GbE] [Throughput: 2.3 Gbps]                  │
│                                                                 │
│ 容量:                                                           │
│   [UniFi Devices: 30] [Client Devices: 300]                    │
│                                                                 │
│ 其他:                                                           │
│   [Integrated Storage] [UI Care]                               │
│                                                                 │
│ 促销:                                                           │
│   [🔥 Black Friday]                                            │
└─────────────────────────────────────────────────────────────────┘
```

### 4.3 Deals 页面（新增）

专门的折扣监控页面。

```
┌─────────────────────────────────────────────────────────────────┐
│ 🔥 当前折扣                                    排序: [折扣最高▾]│
├─────────────────────────────────────────────────────────────────┤
│ ┌───────────────────────────────────────────────────────────┐   │
│ │ [img] Dream Wall          $599 (was $999)  -40%    ✅ 有货│   │
│ │       UDW-US              🔥 Black Friday                 │   │
│ ├───────────────────────────────────────────────────────────┤   │
│ │ [img] Cloud Gateway Max   $179 (was $279)  -36%    ✅ 有货│   │
│ │       UCG-Max             🔥 Black Friday                 │   │
│ ├───────────────────────────────────────────────────────────┤   │
│ │ [img] Dream Router 7      $229 (was $279)  -18%    ✅ 有货│   │
│ │       UDR7-US             🔥 Black Friday                 │   │
│ └───────────────────────────────────────────────────────────┘   │
│                                                                 │
│ 📊 折扣统计                                                     │
│ ┌──────────────┬──────────────┬──────────────┐                 │
│ │ 打折产品数   │ 平均折扣     │ 最高折扣     │                 │
│ │     28       │    25%       │    40%       │                 │
│ └──────────────┴──────────────┴──────────────┘                 │
└─────────────────────────────────────────────────────────────────┘
```

**数据查询**：
```sql
SELECT
  p.id, p.name, p.title, p.image_url,
  pv.sku, pv.current_price, pv.regular_price, pv.discount_percent,
  pv.in_stock
FROM products p
JOIN product_variants pv ON p.id = pv.product_id
WHERE pv.discount_percent > 0
ORDER BY pv.discount_percent DESC;
```

### 4.4 Alerts 页面（新增）

变化监控提醒页面。

```
┌─────────────────────────────────────────────────────────────────┐
│ 🔔 变化提醒                              时间范围: [最近24小时▾]│
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ 💰 价格变化 (5)                                                 │
│ ┌───────────────────────────────────────────────────────────┐   │
│ │ UCG-Max     $279 → $179  ↓ -36%  2小时前                  │   │
│ │ UDR7-US     $279 → $229  ↓ -18%  2小时前                  │   │
│ └───────────────────────────────────────────────────────────┘   │
│                                                                 │
│ 📦 库存变化 (3)                                                 │
│ ┌───────────────────────────────────────────────────────────┐   │
│ │ USW-Pro-48  Out of Stock → In Stock  ✅ 补货  1小时前     │   │
│ │ U7-Pro      In Stock → Out of Stock  ❌ 缺货  3小时前     │   │
│ └───────────────────────────────────────────────────────────┘   │
│                                                                 │
│ 🆕 新增折扣 (2)                                                 │
│ ┌───────────────────────────────────────────────────────────┐   │
│ │ Dream Wall  新增 40% 折扣  2小时前                        │   │
│ └───────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**变化检测查询**：
```sql
-- 价格变化检测 (对比最近两次记录)
WITH ranked AS (
  SELECT
    sku, price, regular_price, recorded_at,
    LAG(price) OVER (PARTITION BY sku ORDER BY recorded_at) as prev_price,
    ROW_NUMBER() OVER (PARTITION BY sku ORDER BY recorded_at DESC) as rn
  FROM variant_history
  WHERE recorded_at > NOW() - INTERVAL '24 hours'
)
SELECT * FROM ranked
WHERE rn = 1 AND price != prev_price;

-- 库存变化检测
WITH ranked AS (
  SELECT
    sku, in_stock, recorded_at,
    LAG(in_stock) OVER (PARTITION BY sku ORDER BY recorded_at) as prev_stock,
    ROW_NUMBER() OVER (PARTITION BY sku ORDER BY recorded_at DESC) as rn
  FROM variant_history
  WHERE recorded_at > NOW() - INTERVAL '24 hours'
)
SELECT * FROM ranked
WHERE rn = 1 AND in_stock != prev_stock;
```

---

## 五、导航结构更新

### 5.1 新 Navbar

```
┌─────────────────────────────────────────────────────────────────┐
│ 🌐 UniFi Monitor    [Dashboard] [Deals (28)] [Alerts (8)]  v2.0│
└─────────────────────────────────────────────────────────────────┘
```

**导航项**：
| 项目 | 路由 | 徽章 |
|------|------|------|
| Dashboard | `/` | - |
| Deals | `/deals` | 打折产品数量 |
| Alerts | `/alerts` | 未读变化数量 |

---

## 六、组件结构

### 6.1 新增组件清单

```
components/
├── common/
│   ├── StatsCard.tsx          # 统计卡片
│   ├── PriceDisplay.tsx       # 价格显示（含折扣）
│   ├── StockBadge.tsx         # 库存状态徽章
│   ├── DiscountBadge.tsx      # 折扣徽章
│   └── PromoTag.tsx           # 促销标签
│
├── dashboard/
│   ├── StatsOverview.tsx      # 统计概览
│   ├── FilterBar.tsx          # 筛选栏
│   ├── QuickFilters.tsx       # 快捷筛选标签
│   └── ProductTable.tsx       # 产品表格
│
├── product/
│   ├── ProductInfo.tsx        # 产品信息
│   ├── VariantSelector.tsx    # 变体选择器
│   ├── VariantTable.tsx       # 变体价格表
│   ├── PriceHistoryChart.tsx  # 价格历史图表
│   ├── StockHistoryChart.tsx  # 库存历史图表
│   ├── DiscountHistory.tsx    # 折扣历史
│   └── ProductFeatures.tsx    # 产品特性标签
│
├── deals/
│   ├── DealsList.tsx          # 折扣列表
│   └── DealsStats.tsx         # 折扣统计
│
└── alerts/
    ├── PriceChangeList.tsx    # 价格变化列表
    ├── StockChangeList.tsx    # 库存变化列表
    └── AlertsFilter.tsx       # 提醒筛选
```

### 6.2 服务层更新

```typescript
// services/storeService.ts

// 获取产品列表（带汇总信息）
export const getProducts = async (filters?: ProductFilters): Promise<Product[]>;

// 获取产品详情（含变体、标签、选项）
export const getProductDetail = async (id: string): Promise<ProductDetail>;

// 获取变体历史
export const getVariantHistory = async (variantId: number): Promise<VariantHistory[]>;

// 获取打折产品
export const getDeals = async (): Promise<ProductVariant[]>;

// 获取价格变化
export const getPriceChanges = async (hours: number): Promise<PriceChange[]>;

// 获取库存变化
export const getStockChanges = async (hours: number): Promise<StockChange[]>;

// 获取统计数据
export const getStats = async (): Promise<DashboardStats>;
```

---

## 七、数据展示优先级

### 7.1 Dashboard 关键信息

| 优先级 | 信息 | 原因 |
|--------|------|------|
| P0 | 折扣状态 + 折扣% | 用户最关心的信息 |
| P0 | 库存状态 | 决定是否能购买 |
| P1 | 价格/价格区间 | 购买决策依据 |
| P1 | 变体数量 | 了解产品复杂度 |
| P2 | 促销标签 | 识别特殊活动 |
| P2 | 最后更新时间 | 数据新鲜度 |

### 7.2 详情页关键信息

| 优先级 | 信息 | 原因 |
|--------|------|------|
| P0 | 变体选择器 + 各变体价格 | 多变体产品核心功能 |
| P0 | 折扣信息（原价、折扣价、%） | 购买决策 |
| P0 | 库存状态 | 购买可行性 |
| P1 | 价格历史图表 | 价格趋势分析 |
| P1 | 折扣历史 | 了解促销规律 |
| P2 | 产品特性标签 | 技术规格参考 |
| P2 | 库存历史 | 补货规律分析 |

---

## 八、待确认事项

- [ ] 是否需要用户登录/个人收藏功能？  暂时不用
- [ ] 是否需要价格/库存变化的邮件/推送通知？  需要考虑，在后续规划版本中。
- [ ] Alerts 页面的变化检测频率？（每次爬虫运行 vs 每日汇总） 每日汇总
- [ ] 是否需要导出功能（CSV/Excel）？   暂时不用
- [ ] 移动端适配优先级？ 暂无

---

**请审阅后告知是否需要调整，确认后我将开始实施。**
